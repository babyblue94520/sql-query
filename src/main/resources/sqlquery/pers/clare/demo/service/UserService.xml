<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE SQL>
<SQL>
	<findAll><![CDATA[
		select u.id
		     ,u.account
		     ,u.type
		     ,u.name
		     ,u.nickname
		     ,u.email
		     ,u.role_id
		     ,u.login_fail_count
		     ,u.locked
		     ,u.enabled
		     ,u.max_reception
		     ,u.update_time
		     ,u.update_user
		     ,u.create_time
		     ,u.create_user
		from `user` u
		left join `role` r on r.id = u.role_id
		where 1=1
			{id}
			{name}
			{roleId}
		 	{account}
	]]></findAll>
</SQL>
